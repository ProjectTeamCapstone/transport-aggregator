# Full local training dependencies.
# Install with:  pip install -r requirements-dev.txt
# Only needed to BUILD the data + LightGBM model locally, NOT for the Vercel deployment.
-r requirements.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
# optional: enables Claude-powered NL parsing (set USE_CLAUDE=1 + ANTHROPIC_API_KEY)
# anthropic>=0.34
