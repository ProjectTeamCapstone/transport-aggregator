[pytest]
# rootdir on sys.path, so `from common...` and `from tests.conftest...` resolve
# without the package having to be installed.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
markers =
    integration: needs a real PostgreSQL. Skipped when one is unreachable.
